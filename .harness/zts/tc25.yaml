pipeline:
  name: ZTS TC25 Conditionally-skipped ancestor
  identifier: zts_tc25_skipped_ancestor
  projectIdentifier: ZTS
  orgIdentifier: default
  description: "A prerequisite ancestor that is itself conditionally skipped (when=false)"
  tags: {}
  stages:
    - stage:
        name: st1
        identifier: st1
        type: Custom
        spec:
          execution:
            steps:
              - step:
                  name: s1
                  identifier: s1
                  type: ShellScript
                  timeout: 10m
                  spec: {shell: Bash, onDelegate: true, source: {type: Inline, spec: {script: echo zts}}}
                  when:
                    stageStatus: Success
                    condition: "false"
              - step: {name: s2, identifier: s2, type: ShellScript, timeout: 10m, spec: {shell: Bash, onDelegate: true, source: {type: Inline, spec: {script: echo zts}}}}
